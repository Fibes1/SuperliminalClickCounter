# SuperliminalClickCounter
A MelonLoader mod to count clicks

In MelonPreferences:
- segmented = whether the click count should be reverted when resetting to checkpoint and restarting level
- alignment = the position of the counter (see https://docs.unity3d.com/2019.1/Documentation/ScriptReference/TextAnchor.html)
